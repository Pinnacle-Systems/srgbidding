This directory is reserved for PostgreSQL migration history.

The legacy MySQL migration history remains in `src/models/migrations`.
Create a fresh PostgreSQL migration chain here with Prisma commands after the provider switch.
